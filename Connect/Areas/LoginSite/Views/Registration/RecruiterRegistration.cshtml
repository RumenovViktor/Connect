@model UserRegistration

<div class="panel panel-default form-group">
    <div class="panel-heading">
        <h4>Register</h4>
    </div>
    <div class="panel-body">
        @using (Ajax.BeginForm("Registration", "Registration", null, new AjaxOptions()
        {
            OnFailure = "loginSiteManager.existingUserMessage",
            OnSuccess = "loginSiteManager.redirectToUrl"
        }))
        {
            @Html.AntiForgeryToken()
            <input type="hidden" value="@UserType.Recruiter" name="UserType" />
            <div class="form-group">
                <div class="col-lg-12">
                    @Html.TextBoxFor(m => m.FirstName, new { @class = "form-control form-group", placeholder = "First name..." })
                </div>
            </div>
            <div class="form-group">
                <div class="col-lg-12">
                    @Html.TextBoxFor(m => m.LastName, new { @class = "form-control form-group", placeholder = "Last name..." })
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-12">
                    @Html.DropDownListFor(x => x.CountryId,
                            new SelectList(Model.Countries.Select(x => new { Value = x.CountryId, Text = x.CountryName }), "Value", "Text"),
                                            "Country", new { @class = "form-control", id = "userCountry" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-lg-12">
                    @Html.TextBoxFor(m => m.Email, new { @class = "form-control form-group", placeholder = "Email...", type = "email" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-lg-12">
                    @Html.TextBoxFor(m => m.Password, new { @class = "form-control form-group", placeholder = "Password...", type = "password" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-lg-12">
                    @Html.TextBoxFor(m => m.ConfirmPassword, new { @class = "form-control form-group", placeholder = "Confirm Password...", type = "password" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-lg-12">
                    <input class="col-md-12 btn btn-primary" type="submit" value="Register" />
                </div>
            </div>
        }
    </div>
</div>